---
openssh_dest: '/etc/ssh'
# Debian/Ubuntu unit — 'ssh.service'. Alias 'sshd.service' был удалён в
# Ubuntu 24.04 minimal cloud images (openssh-server 1:9.6p1-3ubuntu13),
# так что задача 'systemd: name=sshd' падает 'Could not find service sshd'.
openssh_service: 'ssh'
